[package]
name = "rmpv-tests"
version = "0.0.1"
authors = ["Evgeny Safronov <division494@gmail.com>"]
license = "MIT"

[features]
with-serde = ["serde"]

[dependencies]
serde = { version = "^0.8", optional = true }
rmp-serde = { version = "^0.11", path = "../rmp-serde" }
rmpv = { version = "^0.1.0", path = "../rmpv", features = ["with-serde"] }
